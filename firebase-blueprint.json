{
  "entities": {
    "projects": {
      "title": "Projects",
      "description": "Wedding projects and movie specs managed by editors",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "projectName": { "type": "string" },
        "brideName": { "type": "string" },
        "groomName": { "type": "string" },
        "status": { "type": "string" }
      },
      "required": ["id", "projectName"]
    },
    "notifications": {
      "title": "Notifications",
      "description": "Workflow alerts and automated task deadline notifications",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "message": { "type": "string" },
        "type": { "type": "string" },
        "projectId": { "type": "string" },
        "studioId": { "type": "string" },
        "calendarEventId": { "type": "string" },
        "isAutomated": { "type": "boolean" },
        "read": { "type": "boolean" }
      },
      "required": ["id", "title", "message", "type", "read"]
    },
    "calendar": {
      "title": "Calendar Events",
      "description": "Upcoming project shoot, delivery, and revision deadlines",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "start": { "type": "string" },
        "type": { "type": "string" },
        "projectId": { "type": "string" },
        "coupleName": { "type": "string" },
        "color": { "type": "string" }
      },
      "required": ["id", "title", "start"]
    },
    "invoices": {
      "title": "Invoices",
      "description": "GST-compliant luxury bills generated for wedding studio partners",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "projectId": { "type": "string" },
        "coupleName": { "type": "string" },
        "studioId": { "type": "string" },
        "studioName": { "type": "string" },
        "invoiceDate": { "type": "string" },
        "dueDate": { "type": "string" },
        "subtotal": { "type": "number" },
        "gstRate": { "type": "number" },
        "gstAmount": { "type": "number" },
        "discount": { "type": "number" },
        "totalAmount": { "type": "number" },
        "amountPaid": { "type": "number" },
        "balanceDue": { "type": "number" },
        "status": { "type": "string" }
      },
      "required": ["id", "projectId", "studioId", "totalAmount"]
    }
  },
  "firestore": {
    "projects/{projectId}": {
      "schema": "projects",
      "description": "Wedding projects collection"
    },
    "notifications/{notificationId}": {
      "schema": "notifications",
      "description": "System workflow alerts collection"
    },
    "calendar/{eventId}": {
      "schema": "calendar",
      "description": "Calendar events and project deadlines collection"
    },
    "invoices/{invoiceId}": {
      "schema": "invoices",
      "description": "Custom GST invoices collection"
    }
  }
}
